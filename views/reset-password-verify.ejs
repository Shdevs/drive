<!DOCTYPE html>
<html lang="az">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Şifrə Sıfırlama - Drive</title>
    <link rel="icon" type="image/png" href="/img/drivecolor.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        :root {
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --bg-card: #ffffff;
            --text-primary: #212529;
            --text-secondary: #6c757d;
            --border-color: #dee2e6;
            --input-bg: #f1f3f4;
            --gradient-start: #4285f4;
            --gradient-end: #34a853;
            --shadow: rgba(66, 133, 244, 0.12);
            --shadow-hover: rgba(66, 133, 244, 0.18);
        }

        [data-theme="dark"] {
            --bg-primary: #1e1e1e;
            --bg-secondary: #121212;
            --bg-card: #2d2d2d;
            --text-primary: #e0e0e0;
            --text-secondary: #b0b0b0;
            --border-color: #404040;
            --input-bg: #3a3a3a;
            --gradient-start: #3d6bb3;
            --gradient-end: #2d7a3d;
            --shadow: rgba(0, 0, 0, 0.3);
            --shadow-hover: rgba(0, 0, 0, 0.5);
        }

        * {
            transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
        }

        body {
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-end) 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
        }

        .theme-toggle {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255, 255, 255, 0.3);
            color: white;
            padding: 0.75rem 1rem;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1.2rem;
            z-index: 1000;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .theme-toggle:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: rotate(15deg) scale(1.1);
        }

        [data-theme="dark"] .theme-toggle {
            background: rgba(0, 0, 0, 0.3);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .verify-form {
            max-width: 500px;
            width: 100%;
            padding: 40px 35px;
            background: var(--bg-card);
            border-radius: 20px;
            box-shadow: 0 10px 40px var(--shadow);
            animation: fadeIn 0.8s;
            margin: 0 auto;
        }

        .verify-form h2 {
            font-weight: 700;
            letter-spacing: 1px;
            color: var(--text-primary);
            margin-bottom: 1rem;
        }

        .verify-form p {
            color: var(--text-secondary);
            margin-bottom: 2rem;
        }

        .email-display {
            background: var(--input-bg);
            padding: 1rem;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 2rem;
            color: var(--text-primary);
            font-weight: 600;
        }

        .form-control {
            background-color: var(--input-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            padding: 0.75rem 1rem;
            font-size: 1.5rem;
            text-align: center;
            letter-spacing: 0.5rem;
            font-weight: 600;
        }

        .form-control:focus {
            background-color: var(--input-bg);
            border-color: var(--gradient-end);
            color: var(--text-primary);
            box-shadow: 0 0 0 3px rgba(52, 168, 83, 0.15);
        }

        .form-control::placeholder {
            color: var(--text-secondary);
            letter-spacing: 0;
        }

        .btn-primary {
            background: linear-gradient(90deg, var(--gradient-start), var(--gradient-end));
            border: none;
            font-weight: 600;
            letter-spacing: 1px;
            padding: 0.75rem;
            transition: all 0.3s ease;
            border-radius: 10px;
        }

        .btn-primary:hover {
            box-shadow: 0 6px 20px var(--shadow-hover);
            transform: translateY(-2px);
            background: linear-gradient(90deg, var(--gradient-end), var(--gradient-start));
        }

        .btn-link {
            color: var(--gradient-start);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .btn-link:hover {
            color: var(--gradient-end);
            text-decoration: underline;
        }

        .alert {
            border-radius: 10px;
            margin-bottom: 1.5rem;
        }

        .countdown {
            color: var(--text-secondary);
            font-size: 0.875rem;
            margin-top: 0.5rem;
        }

        @keyframes fadeIn {
            from { 
                opacity: 0; 
                transform: translateY(30px); 
            }
            to { 
                opacity: 1; 
                transform: none; 
            }
        }

        @media (max-width: 576px) {
            .verify-form {
                padding: 30px 25px;
            }
            
            .theme-toggle {
                top: 15px;
                right: 15px;
                padding: 0.6rem 0.8rem;
                font-size: 1rem;
            }

            .form-control {
                font-size: 1.2rem;
                letter-spacing: 0.3rem;
            }
        }
    </style>
</head>
<body>
    <button class="theme-toggle" id="themeToggle" aria-label="Tema dəyişdir">
        <i class="bi bi-moon-fill" id="themeIcon"></i>
    </button>

    <div class="verify-form">
        <h2 class="text-center mb-4">
            <i class="bi bi-shield-lock-fill"></i> Şifrə Sıfırlama
        </h2>
        <p class="text-center">
            <strong id="emailDisplay"></strong> ünvanına doğrulama kodu göndərdik. Zəhmət olmasa email qutunuzu yoxlayın və aşağıdakı kodu daxil edin.
        </p>
        
        <div class="email-display">
            <i class="bi bi-envelope"></i> <span id="emailDisplay2"></span>
        </div>

        <div id="alertContainer"></div>

        <form id="verifyForm">
            <div class="mb-3">
                <input 
                    type="text" 
                    class="form-control text-center" 
                    id="code" 
                    name="code" 
                    placeholder="000000" 
                    required 
                    maxlength="6"
                    pattern="[0-9]{6}"
                    autocomplete="off"
                >
                <div class="countdown text-center" id="countdown"></div>
            </div>
            <button type="submit" class="btn btn-primary w-100 mb-3">
                <i class="bi bi-check-circle-fill"></i> Doğrula
            </button>
        </form>

        <div class="text-center">
            <button type="button" class="btn btn-link" id="resendBtn">
                <i class="bi bi-arrow-clockwise"></i> Kodu yenidən göndər
            </button>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // URL'den email'i al
        const urlParams = new URLSearchParams(window.location.search);
        const email = urlParams.get('email') || '';

        if (email) {
            document.getElementById('emailDisplay').textContent = email;
            document.getElementById('emailDisplay2').textContent = email;
        }

        // Dark Mode Functionality
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');
        const html = document.documentElement;

        const currentTheme = localStorage.getItem('theme') || 'light';
        
        if (currentTheme === 'dark') {
            html.setAttribute('data-theme', 'dark');
            themeIcon.classList.remove('bi-moon-fill');
            themeIcon.classList.add('bi-sun-fill');
        }

        themeToggle.addEventListener('click', () => {
            const currentTheme = html.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            html.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            
            if (newTheme === 'dark') {
                themeIcon.classList.remove('bi-moon-fill');
                themeIcon.classList.add('bi-sun-fill');
            } else {
                themeIcon.classList.remove('bi-sun-fill');
                themeIcon.classList.add('bi-moon-fill');
            }
        });

        // Kod input sadece sayı kabul etsin
        const codeInput = document.getElementById('code');
        codeInput.addEventListener('input', function(e) {
            this.value = this.value.replace(/[^0-9]/g, '');
        });

        // Geri sayım (10 dakika)
        let timeLeft = 600;
        const countdownEl = document.getElementById('countdown');
        const resendBtn = document.getElementById('resendBtn');
        
        function updateCountdown() {
            const minutes = Math.floor(timeLeft / 60);
            const seconds = timeLeft % 60;
            countdownEl.textContent = `Kod ${minutes}:${seconds.toString().padStart(2, '0')} dəqiqə ərzində etibarlıdır`;
            
            if (timeLeft > 0) {
                timeLeft--;
                setTimeout(updateCountdown, 1000);
            } else {
                countdownEl.textContent = 'Kodun müddəti bitib. Yenidən göndərin.';
                countdownEl.style.color = '#ea4335';
            }
        }
        updateCountdown();

        // Alert gösterme fonksiyonu
        function showAlert(message, type) {
            const alertContainer = document.getElementById('alertContainer');
            alertContainer.innerHTML = `
                <div class="alert alert-${type} alert-dismissible fade show" role="alert">
                    ${message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            `;
        }

        // Yeniden gönder butonu
        resendBtn.addEventListener('click', async function() {
            this.disabled = true;
            this.innerHTML = '<i class="bi bi-hourglass-split"></i> Göndərilir...';
            
            try {
                const response = await fetch('/resend-reset-code', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ email: email })
                });

                const data = await response.json();
                
                if (data.success) {
                    showAlert('Doğrulama kodu yenidən göndərildi!', 'success');
                    timeLeft = 600;
                    updateCountdown();
                    countdownEl.style.color = '';
                } else {
                    showAlert('Xəta: ' + (data.message || 'Kod göndərilə bilmədi'), 'danger');
                }
            } catch (error) {
                showAlert('Xəta: Kod göndərilə bilmədi', 'danger');
            } finally {
                this.disabled = false;
                this.innerHTML = '<i class="bi bi-arrow-clockwise"></i> Kodu yenidən göndər';
            }
        });

        // Form gönderimi
        const verifyForm = document.getElementById('verifyForm');
        verifyForm.addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const code = codeInput.value.trim();
            if (code.length !== 6) {
                showAlert('Zəhmət olmasa 6 rəqəmli kodu daxil edin', 'warning');
                return;
            }

            const submitBtn = this.querySelector('button[type="submit"]');
            const originalBtnText = submitBtn.innerHTML;
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="bi bi-hourglass-split"></i> Yoxlanılır...';
            
            try {
                const response = await fetch('/reset-password-verify', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ code: code, email: email })
                });

                const contentType = response.headers.get('content-type');
                
                if (contentType && contentType.includes('application/json')) {
                    const data = await response.json();
                    if (data.success) {
                        window.location.href = data.redirect || '/reset-password?token=' + encodeURIComponent(data.token);
                        return;
                    } else {
                        showAlert(data.message || 'Xəta: Doğrulama zamanı problem yarandı', 'danger');
                        submitBtn.disabled = false;
                        submitBtn.innerHTML = originalBtnText;
                        return;
                    }
                }

                if (!response.ok) {
                    try {
                        const errorText = await response.text();
                        showAlert(errorText || 'Xəta: Doğrulama zamanı problem yarandı', 'danger');
                    } catch (e) {
                        showAlert('Xəta: Doğrulama zamanı problem yarandı', 'danger');
                    }
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = originalBtnText;
                    return;
                }

                window.location.href = '/reset-password';
            } catch (error) {
                console.error('Doğrulama hatası:', error);
                showAlert('Xəta: Doğrulama zamanı problem yarandı', 'danger');
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalBtnText;
            }
        });
    </script>
</body>
</html>

